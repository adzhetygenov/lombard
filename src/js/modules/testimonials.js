export function init() {
  const trigger = document.querySelector(".js-trigger");
  const items = Array.from(document.querySelectorAll(".masthead__function"));

  //   trigger.addEventListener("mouseover", (e) => {
  //     items.map((el, index) => {
  //       el.classList.add("masthead__function--show");

  //       el.style.transitionDelay = `${index / 2}s`;
  //     });
  //   });

  //   trigger.addEventListener("mouseout", (e) => {
  //     items.map((el, index) => {
  //       el.classList.remove("masthead__function--show");

  //       el.style.transitionDelay = `${index / 2}s`;
  //     });
  //   });
}
